# Plan

Paper 83 `robot_skill_half_life_under_shift` completed the 2026-06-21 v5 expanded submission-readiness audit.

Frozen execution plan:

1. Freeze the expanded protocol before edits in `docs/paper83_expanded_submission_plan_20260621.md`.
2. Run the full deterministic CPU-only skill-survival benchmark from source.
3. Audit main, hard aggregate, paired, ablation, stress, fixed-risk, and negative-case outputs.
4. Apply the ICLR-main evidence gate without overclaiming local synthetic evidence.
5. Generate a 25+ page ICLR-style manuscript with bright boxed clickable citations.
6. Validate row counts, page count, citation/link settings, LaTeX log cleanliness, Downloads-only PDF placement, Desktop exclusion, and SHA256.
7. Preserve the terminal decision as `KILL_ARCHIVE` because the v5 method loses to `cvar_lifetime_guard` and fails margin, paired, ablation, fixed-risk, and stress gates.

Canonical PDF: `C:/Users/wangz/Downloads/83.pdf`.
